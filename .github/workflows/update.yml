name: Update V2Ray Subscriptions

on:
  schedule:
    # هر ۶ ساعت یک‌بار به‌روزرسانی می‌شود (می‌توانید زمان دلخواه را تنظیم کنید)
    - cron: '0 */6 * * *'
  workflow_dispatch: # امکان اجرای دستی از تب Actions

jobs:
  update-configs:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Run script
      run: |
        python main.py

    - name: Commit and push changes
      run: |
        git config user.name "bot"
        git config user.email "bot@users.noreply.github.com"
        
        # دریافت آخرین تغییرات از سرور قبل از کامیت و پوش برای جلوگیری از خطای reject
        git pull origin main --rebase || true
        
        git add output/
        git diff --staged --quiet || git commit -m "Update Vless Configs $(date -u '+%Y-%m-%d %H:%M UTC')"
        git push origin main
